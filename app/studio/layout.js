export const metadata = { title: 'Alginate Compare Studio' }

// The Studio manages its own <html>/<body> — opt out of the root layout styles
export default function StudioLayout({ children }) {
  return children
}
